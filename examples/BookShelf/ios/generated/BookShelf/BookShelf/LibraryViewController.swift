//
//  LibraryViewController.swift"
//  Library//
//  Automatically generated by MobileCodeGenerator 3.
//  Copyright © 2017 . All rights reserved.
//
import UIKit

class LibraryViewController: UIViewController, UITableViewDelegate, UITableViewDataSource
 {

	@IBOutlet weak var trcjpListView: UITableView!
	
	var trcjpListViewContents: [String] = [
		"shelf1",
	]




	
	override func viewDidLoad() {
	    super.viewDidLoad()

		self.trcjpListView.delegate = self
		self.trcjpListView.dataSource = self

	}
	
	override func viewDidAppear(_ animated: Bool) {
	    super.viewDidAppear(animated)
	}
	
	override func viewDidDisappear(_ animated: Bool) {
		super.viewDidDisappear(animated)
	}



	func numberOfSections(in tableView: UITableView) -> Int {
	    return 1
	}

	func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
	    // Return the number of rows
	    if tableView == self.trcjpListView {
	        return self.trcjpListViewContents.count;
	    }
	    return 0 
	}

	func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
	    // Configure the cell...
	    if tableView == self.trcjpListView {
	    
	    	let trcjpListViewCell = tableView.dequeueReusableCell(withIdentifier: "trcjpListViewTableViewCell", for: indexPath) as! SimpleTableViewCell
	        trcjpListViewCell.label.text = self.trcjpListViewContents[indexPath.row]
	        return trcjpListViewCell
	        
	    }
	
	    return UITableViewCell()
	}
	
	
	func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
	    if tableView == self.trcjpListView {
	    }
	    tableView.deselectRow(at: indexPath, animated: true)
	}
	
	override func viewWillAppear(_ animated: Bool) {
		super.viewWillAppear(animated)
	}
	
	override func viewWillDisappear(_ animated: Bool) {
		super.viewWillDisappear(animated)
	}
	
	override func didReceiveMemoryWarning() {
	    super.didReceiveMemoryWarning()
	    // Dispose of any resources that can be recreated.
	}
	
	override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
		//You can reference to the next ViewController and the selected UITableViewRow     
		if segue.identifier == "SegueIdentifierFromtrcjpListViewToshelfDetailViewControllerID" {
 	        let index = self.trcjpListView.indexPathForSelectedRow!
			let destination = segue.destination as! shelfDetailViewController
            // Pass the selected cell content to destination ...
		}
	}
}
