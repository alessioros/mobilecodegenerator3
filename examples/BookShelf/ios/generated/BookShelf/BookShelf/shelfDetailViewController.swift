//
//  shelfDetailViewController.swift"
//  shelfDetail//
//  Automatically generated by MobileCodeGenerator 3.
//  Copyright © 2017 . All rights reserved.
//
import UIKit

class shelfDetailViewController: UIViewController, UITableViewDelegate, UITableViewDataSource
 {

	@IBOutlet weak var bookList: UITableView!
	
	var bookListContents: [String] = [
	]




	
	override func viewDidLoad() {
	    super.viewDidLoad()

		self.bookList.delegate = self
		self.bookList.dataSource = self

	}
	
	override func viewDidAppear(_ animated: Bool) {
	    super.viewDidAppear(animated)
	}
	
	override func viewDidDisappear(_ animated: Bool) {
		super.viewDidDisappear(animated)
	}



	func numberOfSections(in tableView: UITableView) -> Int {
	    return 1
	}

	func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
	    // Return the number of rows
	    if tableView == self.bookList {
	        return self.bookListContents.count;
	    }
	    return 0 
	}

	func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
	    // Configure the cell...
	    if tableView == self.bookList {
	    
	    	let bookListCell = tableView.dequeueReusableCell(withIdentifier: "bookListTableViewCell", for: indexPath) as! SimpleTableViewCell
	        bookListCell.label.text = self.bookListContents[indexPath.row]
	        return bookListCell
	        
	    }
	
	    return UITableViewCell()
	}
	
	
	func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
	    if tableView == self.bookList {
	    }
	    tableView.deselectRow(at: indexPath, animated: true)
	}
	
	override func viewWillAppear(_ animated: Bool) {
		super.viewWillAppear(animated)
	}
	
	override func viewWillDisappear(_ animated: Bool) {
		super.viewWillDisappear(animated)
	}
	
	override func didReceiveMemoryWarning() {
	    super.didReceiveMemoryWarning()
	    // Dispose of any resources that can be recreated.
	}
	
	override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
		//You can reference to the next ViewController and the selected UITableViewRow     
		if segue.identifier == "SegueIdentifierFrombookListTobookDetailViewControllerID" {
 	        let index = self.bookList.indexPathForSelectedRow!
			let destination = segue.destination as! bookDetailViewController
            // Pass the selected cell content to destination ...
		}
	}
}
